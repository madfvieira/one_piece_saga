/*
 * classNames object
 *   ENUM mapping for all classNames we will use.  When these classes are styled
 *   or used by JS in someway they MUST be fetched via this ENUM.
 */
const classNames = {
    'OPcard' : {
        'wrapper'      : 'op_card_wrapper',
        'title'        : 'op_card_title',
        'description'  : 'op_card_description',
        'saga_and_arc' : 'op_card_saga_and_arc',
    },
};

export default classNames;
